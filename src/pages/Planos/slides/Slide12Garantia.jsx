import Guarantee from '../../../components/Guarantee';

const Slide12Garantia = ({ step }) => (
  <div className="slide-section-wrap guarantee-wrap has-steps">
    <Guarantee step={step} />
  </div>
);

Slide12Garantia.steps = 2;

export default Slide12Garantia;
